/// Shared version string for all Ampere executables.
///
/// This is the single source of truth for the version line printed by
/// `ampered`, `Ampere`, and `ampere-cli`. Bump it here.
public enum Version {
    public static let string = "0.0.1"
}
